# Deprecated UX flow reference

The Agent Builder no longer uses phases, state screens, command words, or
mandatory progression. Use `conversation-flow.md` for user-facing responses and
`validation.md` for tests and diagnosis.
