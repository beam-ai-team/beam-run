# Validate an agent

This is an on-demand validation playbook. Run the scenarios the user asks for;
when they request general validation, select representative scenarios for the
actual use case. State expected behavior, inspect node-level outputs and routing,
then explain any mismatch with a concise cause and proposed repair.

Do not require a fixed five-case suite, a test command word, or a particular
point in the conversation before testing.
